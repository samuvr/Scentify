/**
 * Sustituto de `server-only` para los tests.
 *
 * El paquete real lanza al importarse fuera de un componente de servidor, que
 * es justo lo que hace un test. Ver el alias en vitest.config.ts.
 */
export {};
